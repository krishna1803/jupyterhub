apiVersion: apps/v1
kind: Deployment
metadata:
  name: jupyterhub-manager-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jupyterhub-manager-ui
  template:
    metadata:
      labels:
        app: jupyterhub-manager-ui
    spec:
      containers:
        - name: ui
          image: your-registry/jupyterhub-manager-ui:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: JUPYTERHUB_MANAGER_JUPYTERHUB_URL
              value: "https://hub.example.com"
            - name: JUPYTERHUB_MANAGER_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: jhub-admin-token
                  key: token
          ports:
            - containerPort: 8501
          readinessProbe:
            httpGet:
              path: /
              port: 8501
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: jupyterhub-manager-ui
spec:
  selector:
    app: jupyterhub-manager-ui
  ports:
    - port: 80
      targetPort: 8501
